#ifndef VECTOR_VECTORCONSTANTS_H
#define VECTOR_VECTORCONSTANTS_H

#include "Vector.h"

VECTOR_API const Vector zero = Vector(Number(0), Number(0));
VECTOR_API const Vector one = Vector(Number(1), Number(1));

#endif //VECTOR_VECTORCONSTANTS_H
